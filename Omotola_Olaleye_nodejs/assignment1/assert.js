// A set of assertion functions useful for testing. The assert module provides a way of testing expressions. If the expression evaluates to 0, or false, an assertion failure is being caused, and the program is terminated.
var assert = require("assert");
assert(40 > 90, "This expression is false");
